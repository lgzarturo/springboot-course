<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}</pattern>
        </encoder>
    </appender>

    <!-- Sentry Appender - Solo si DSN está configurado -->
    <springProperty scope="context" name="sentryDsn" source="sentry.dsn" defaultValue=""/>
    <springProperty scope="context" name="sentryEnvironment" source="sentry.environment" defaultValue="development"/>
    <springProperty scope="context" name="sentryRelease" source="sentry.release" defaultValue="0.0.1"/>

    <if condition='property("sentryDsn").length() > 0'>
        <then>
            <appender name="SENTRY" class="io.sentry.logback.SentryAppender">
                <options>
                    <dsn>${sentryDsn}</dsn>
                    <environment>${sentryEnvironment}</environment>
                    <release>${sentryRelease}</release>
                    <sendDefaultPii>false</sendDefaultPii>
                    <attachStacktrace>true</attachStacktrace>
                    <attachThreads>true</attachThreads>
                </options>
                <!-- Capturar solo WARN y ERROR -->
                <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                    <level>WARN</level>
                </filter>
            </appender>
        </then>
    </if>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
    <!-- <if> surronding <root> is fine -->
    <if condition='property("sentryDsn").length() > 0'>
        <then>
            <root>
                <appender-ref ref="SENTRY" />
            </root>
        </then>
    </if>

    <!-- Application Loggers -->
    <logger name="com.lgzarturo.springbootcourse" level="DEBUG"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.orm.jdbc.bind" level="TRACE"/>
</configuration>
